{% if bridge_bridges | selectattr('ipv4', 'defined') | list %}
net.ipv4.ip_forward = 1
{% endif %}
{% if bridge_bridges | selectattr('ipv6', 'defined') | list %}
net.ipv6.conf.all.forwarding = 1
{% endif %}
