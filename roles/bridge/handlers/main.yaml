---
- name: Restart dnsmasq
  ansible.builtin.service:
    name: dnsmasq
    state: restarted
  tags: bridge

- name: Reload sysctl
  ansible.builtin.command: sysctl --load /etc/sysctl.d/50-ip-forwarding.conf
  tags: bridge
